# 04 — Winner switching

**Result: NOT MEASURABLE**

Winner switching requires **Date × Query × Page** (or at least daily primary URL per query). Available date sheet is site totals only (clicks/impressions/position per day), not per URL or query.

| Signal | Status |
|--------|--------|
| Primary URL switch count | Unknown |
| Switch pairs | Unknown |
| Position volatility by query | Unknown |
| Click volatility by URL×query | Unknown |

Daily chart shows mostly zero-click days with sparse impressions — insufficient to infer URL selection.

**Do not claim Google “switches often” or “never switches” from this export.**
