# Choose ONE provider. If both are set, GTM takes precedence to prevent duplicate events.
# PUBLIC_GTM_CONTAINER_ID=GTM-XXXXXXX
# PUBLIC_GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# With neither Google ID set, the production Worker uses the consent-gated
# first-party Cloudflare Analytics Engine collector automatically.
